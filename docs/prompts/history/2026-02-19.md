# 2026-02-19

Prompts received:

1. Use the Presence repository (https://github.com/DAWS25/Presence) as a template/standard/reference implementation for future requests.
   - Applied: The Presence repository was indexed and will be used as a reference.

2. Create a `docs` folder with a `history` subfolder to keep a track record; keep assistant notes there.
   - Applied: `docs/history/README.md` and dated entry created.

3. Create a `prompts` folder with a `history` of prompts (now moved under `docs`).
   - Applied: `docs/prompts/history/README.md` and dated entry created.
